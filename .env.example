###############################################
# EducAid Environment Configuration (Example) #
###############################################

# ------------------ DATABASE ------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=educaid
DB_USER=postgres
DB_PASSWORD=postgres_dev_2025

# ------------------ reCAPTCHA v3 --------------
# Obtain from https://www.google.com/recaptcha/admin/create (use v3)
RECAPTCHA_SITE_KEY=replace_me_site_key
RECAPTCHA_SECRET_KEY=replace_me_secret_key

# ------------------ SMTP / Email --------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your_smtp_username_or_email
SMTP_PASSWORD=your_app_password_or_secret
SMTP_FROM_EMAIL=educaid@example.com
SMTP_FROM_NAME="EducAid"

# ------------------ OPTIONAL ------------------
# Adjust minimum reCAPTCHA score threshold if needed (handled in code usage)
# RECAPTCHA_MIN_SCORE=0.5

# Any additional custom application secrets can be added below:
# APP_ENV=development
# APP_DEBUG=true

# After copying this file to .env, replace placeholder values.
